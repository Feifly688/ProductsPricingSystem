<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
    <!--查询所有-->
    <select id="selectAll" resultType="com.example.entity.User">
        select * from user
        <where>
            <if test="username != null and username != ''">and username like concat('%', #{username}, '%')</if>
            <if test="phone != null and phone != ''">and phone like concat('%', #{phone}, '%')</if>
            <if test="email != null and email != ''">and email like concat('%', #{email}, '%')</if>
        </where>
        order by id
    </select>
    <!--根据ID查询-->
    <select id="selectById" resultType="com.example.entity.User">
        select *
        from user
        where id = #{id}
    </select>
    <!--根据用户名查询-->
    <select id="selectByUsername" resultType="com.example.entity.User">
        select *
        from user
        <where>
            <if test="username != null">and username like #{username}</if>
        </where>
    </select>
    <!--根据电话查询-->
    <select id="selectByPhone" resultType="com.example.entity.User">
        select *
        from user
        <where>
            <if test="phone != null">and phone = #{phone}</if>
        </where>
    </select>
    <!--新增-->
    <insert id="insert" parameterType="com.example.entity.User" useGeneratedKeys="true">
        insert into user(username, password, name, avatar, role, email, phone, sex)
        values (#{username}, #{password}, #{name}, #{avatar}, #{role}, #{email}, #{phone}, #{sex})
    </insert>
    <!--修改数据-->
    <update id="updateById" parameterType="com.example.entity.User">
        update user
        set username = #{username},
            password = #{password},
            name     = #{name},
            avatar   = #{avatar},
            role     = #{role},
            email    = #{email},
            phone    = #{phone},
            sex      = #{sex}
        where id = #{id}
    </update>
    <!--根据id删除-->
    <delete id="deleteById" parameterType="com.example.entity.User">
        delete
        from user
        where id = #{id}
    </delete>
</mapper>
